{% extends 'layout.html.twig' %}
{% block main %}
    <h3 id="titulo_ta_po" class="text-center">NUEVO TALLER.</h3>
    <div class="container col-lg-10">
        {{ form_start(formulario) }}
        {{ form_widget(formulario) }}
        {{ form_end(formulario) }}
    </div>
{% endblock %}